<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to Rails.application.class.module_parent.name, root_path, class: 'navbar-brand' %>
    <button aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler" data-bs-target="#navbarNavAltMarkup" data-bs-toggle="collapse" type="button">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse">
      <div class="navbar-nav">
        <% if user_signed_in? %>
          <li>
            <%= link_to t('layout.links.logout'), destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %>
          </li>
        <% else %>
          <li>
            <%= link_to t('layout.links.login'), new_user_session_path, class: "nav-link" %>
          </li>
          <li>
            <%= link_to t('layout.links.signup'), new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </div>
    </div>
  </div>
</nav>
