<p style="color: green"><%= notice %></p>

<%= render @account %>

<div>
  <%= link_to t('.show_transaction'), account_transactions_path(account_id: @account.id)%>
</div>
<div>
  <%= link_to t('.show_accounts'), accounts_path %>
</div>
